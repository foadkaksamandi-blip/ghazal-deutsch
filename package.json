{
  "name": "ghazal-deutsch",
  "version": "2.0.0",
  "private": true,
  "description": "Offline-first German acquisition system for migration, real life, university, career and exams",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check app/src/main/assets/core.js && node --check app/src/main/assets/course-data.js && node --check app/src/main/assets/app.js && node --check app/src/main/assets/os-extension.js"
  }
}
